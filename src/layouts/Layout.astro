---
import ChatBubble from "../components/chat/chat";
import HeaderB from "./headers/headerB/index.jsx";
import FooterA from "../layouts/footers/footerA/index.astro";
import MobileCallBanner from "../components/phoneBanner";
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  url?: string;
  image?: string;
  type?: 'website' | 'article';
  publishedTime?: string; // ISO for articles/blog
  noindex?: boolean;
}

const {
  title = "Laser Cutting Services Manchester",
  description = "High-precision CNC laser cutting in Manchester for steel, aluminium, and stainless. Same-day quotes, fast turnaround, burr-free edges, EN1090 CE/UKCA certified. From prototypes to full production runs.",
  url = Astro.url.toString(),
  image = '/og-default.jpg',
  type = 'website',
  publishedTime,
  noindex = false
} = Astro.props;

const siteName = 'Monton Metalwork';
const canonical = url;
const locale = 'en_GB';
---

<!DOCTYPE html>
<html lang="en-GB">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Basic -->
    <title>{title ? `${title} | ${siteName}` : siteName}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <meta name="robots" content={noindex ? 'noindex,nofollow' : 'index,follow'} />

    <!-- Favicon -->
    <link rel="icon" href="/favicon2.png" type="image/x-icon" />

    <!-- Open Graph -->
    <meta property="og:type" content={type} />
    <meta property="og:site_name" content={siteName} />
    <meta property="og:title" content={title ?? siteName} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={image} />
    <meta property="og:locale" content={locale} />
    {type === 'article' && publishedTime && (
      <meta property="article:published_time" content={publishedTime} />
    )}

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title ?? siteName} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={image} />


    <!-- Perf -->
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-KC477V1MJP"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-KC477V1MJP');
    </script>

  </head>
  <body>
    <!-- <SiteBanner client:load message="📢 New opening hours starting next week: Mon–Fri, 7AM–7PM!" /> -->

    <MobileCallBanner client:load />
    <HeaderB client:load />

    <main class="main-content">
      <slot />
    </main>

    <FooterA />

    <!-- My analytics code -->
     <script>
      (function () {
        function notify(where) {
          try {
            var canonical = document.querySelector('link[rel="canonical"]');
            var url = canonical ? new URL(canonical.href, location.href).toString() : location.href;
            parent.postMessage({ type: 'site:navigate', url, where: where || 'load' }, '*');
          } catch (e) {}
        }

        // initial + SPA changes
        var _ps = history.pushState, _rs = history.replaceState;
        history.pushState = function(){ var r=_ps.apply(this, arguments); notify('pushState'); return r; };
        history.replaceState = function(){ var r=_rs.apply(this, arguments); notify('replaceState'); return r; };
        window.addEventListener('popstate', function(){ notify('popstate'); });
        window.addEventListener('hashchange', function(){ notify('hashchange'); });

        if (document.readyState === 'loading') {
          document.addEventListener('DOMContentLoaded', function(){ notify('domcontentloaded'); });
        } else {
          notify('ready');
        }
      })();

        (function(h,o,t,j,a,r){
          h.hj=h.hj||function(){(h.hj.q=h.hj.q||[]).push(arguments)};
          h._hjSettings={hjid:6488831,hjsv:6};
          a=o.getElementsByTagName('head')[0];
          r=o.createElement('script');r.async=1;
          r.src=t+h._hjSettings.hjid+j+h._hjSettings.hjsv;
          a.appendChild(r);
        })(window,document,'https://static.hotjar.com/c/hotjar-','.js?sv=');
    </script>
  </body>
</html>